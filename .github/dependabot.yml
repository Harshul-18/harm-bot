version: 2
updates:
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: monthly
    open-pull-requests-limit: 5
    ignore:
      # Serialized estimators require the training-time-compatible runtime.
      - dependency-name: scikit-learn
      - dependency-name: numpy
